//NOTE: Code is currently very incomplete, just messing around

import java.util.ArrayList;
import java.util.Scanner;

public class Lexer {

  public static void main(String[] args) {

    //Basic list stuff to handle our input and token dump
    Scanner tokenList = new Scanner(System.in);
    ArrayList<Tokens> = new ArrayList<>();
    //Possible matcher here??? idk

  for(int i = 0; i < tokenList.length(); i++) {
    //Iterate through the input with scanner, pick out tokens
      //Include special cases like $ and comment/string stuff
    //Accomodate for errors
    
  }
  }
}
